{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(jq -r '.tool_input.file_path'); case \"$file\" in *.swift) which swiftlint >/dev/null && swiftlint lint --quiet --path \"$file\" 2>/dev/null || true ;; esac"
          }
        ]
      }
    ]
  }
}
